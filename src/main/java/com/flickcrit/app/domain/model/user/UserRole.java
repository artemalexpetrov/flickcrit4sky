package com.flickcrit.app.domain.model.user;

/**
 * Represents the roles that can be assigned to a user within the system.
 * The roles define the level of access and permissions available to a user.
 */
public enum UserRole {
    ADMIN,
    USER
}
